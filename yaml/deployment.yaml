kind: Deployment
apiVersion: apps/v1
metadata:
# set your lab user name
  name: gil-hiye-world
spec:
  replicas: 3
  selector:
    matchLabels:
# set your lab user name
      app: gil-hiye-world
  template:
    metadata:
      labels:
# set your lab user name
        app: gil-hiye-world
    spec:
      containers:
        - resources: {}
          terminationMessagePath: /dev/termination-log
          name: hiye-world
          ports:
            - containerPort: 8080
              protocol: TCP
          imagePullPolicy: IfNotPresent
# update with the image you build in part 1
          image: 'quay.io/ahalan/bechi:v1'
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  revisionHistoryLimit: 10