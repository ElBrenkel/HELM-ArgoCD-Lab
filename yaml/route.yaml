kind: Route
apiVersion: route.openshift.io/v1
metadata:
# set your user name
  name:  user22-hiye-world
spec:
  to:
    kind: Service
# set your user name
    name:  user22-hiye-world
    weight: 100
  port:
    targetPort: 8080
  wildcardPolicy: None